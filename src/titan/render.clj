(ns titan.render)

;; ---------------------------------------------------------------------------
;; content rendering

(defn render-markup [m]
  ; TODO: actually render the markup
  (println)
  (println m)
  (println))
